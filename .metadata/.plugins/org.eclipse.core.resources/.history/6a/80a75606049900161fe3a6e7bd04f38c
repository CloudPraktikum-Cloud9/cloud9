package message;

public enum MessageType {
	TELNET(1);
	
	private final int type;
	private final long maxSize;
	
	MessageType(int type) {
		this.type = type;
		if (type == 1) { this.maxSize = 128;}
		
	}
	
	public long getMaxSize() {
		
	}
	
}
