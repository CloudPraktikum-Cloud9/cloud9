package ui;

import communication.Connection;
import communication.ConnectionImpl;

public class Application {
	private enum name {
		TELNET;	
	}
	
	public static void main(String[] args) 
	{
		Connection connection = new ConnectionImpl();
		CommandHandler commandHandler = new CommandHandler(connection);
		Console console = new Console(commandHandler, this.name);
		console.start();
		System.exit(0);
	}
}